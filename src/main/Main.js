import HeroSection from '../components/heroSection';

export default function Main() {
    return (
        <section>
            <HeroSection />
            <HeroSection />
        </section>
    );
}
